{
  "version": "0.0.1",
  "name": "relight",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "lint": "bunx @biomejs/biome lint index.ts",
    "build": "bun build ./index.ts --outdir ./dist",
    "release": "bun run build && changeset publish && git push --follow-tags"
  },
  "devDependencies": {
    "@biomejs/biome": "1.3.1",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@changesets/cli": "^2.26.2",
    "@types/react": "^18.2.31",
    "@types/react-dom": "^18.2.14",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
